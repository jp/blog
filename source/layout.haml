!!!
%html
  %head
    %meta{charset: "utf-8"}/
    %meta{content: "IE=edge;chrome=1", "http-equiv" => "X-UA-Compatible"}/
    = stylesheet_link_tag "normalize", "all"
    = javascript_include_tag  "all"
  %body
    .container
      .row
        .nav.col-md-4
          .welcome
            %a{href: "/"}
              %i.title.fa.fa-rocket
              %h1 the middleman blog
            %p.doc
              = link_to "by Julien Pellet", "https://twitter.com/julienpellet"
          %aside
            %h2 Recent Articles
            %ol
              - blog.articles[0...10].each do |article|
                %li
                  = link_to article.title, article
                  %span= article.date.strftime('%b %e')
            .hidden-sm.hidden-xs
              %h2 Tags
              %ol
                - blog.tags.each do |tag, articles|
                  %li
                    = link_to tag, tag_path(tag)
                    (#{articles.size})
        .col-md-8
          #main{role: "main"}
            ~ yield
